// Khai bao bien
      // let dataLet = 2
      // const dataConst = 3
      var data = 1 // Khai bao bien pho bien
      var data2 = 2
      var total = data + data2
      console.log(total)

      let x = 1
      let y = 2
      let z = x + y
      console.log(z)

      //Khác biệt giữa var và let
      if (false) {
        let text = 'Học js dễ lắm'
        console.log(text)
        text = 'Thay đổi học js cũng dễ sơ sơ thôi'
        console.log(text)
        // var textVar = 'Hoc js de lam theo cach khai bao var'
        // console.log(textVar)
      }

      // console.log(textVar)
      // console.log(text)

      const constant = 'du lieu khong doi'
      console.log(constant)
      constant = 'thay doi du lieu'
      
      // Khai báo tên biến bao gồm chữ cái, số, _, $
      // Chứ bắt đầu là chữ cái thường: number, human, numberVar, numberVarText, numberLength
      // numberVar <> number_var
      // $NumberVar, _text, __text

      // Khai báo sai: NumberVar, 2var, 3Var